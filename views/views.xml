<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="offer_stats.list">
      <field name="name">offerStats list</field>
      <field name="model">offer_stats.offer_stats</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="offer_stats.action_window">
      <field name="name">offerStats window</field>
      <field name="res_model">offer_stats.offer_stats</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="offer_stats.action_server">
      <field name="name">offerStats server</field>
      <field name="model_id" ref="model_offer_stats_offer_stats"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="offerStats" id="offer_stats.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="offer_stats.menu_1" parent="offer_stats.menu_root"/>
    <menuitem name="Menu 2" id="offer_stats.menu_2" parent="offer_stats.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="offer_stats.menu_1_list" parent="offer_stats.menu_1"
              action="offer_stats.action_window"/>
    <menuitem name="Server to list" id="offer_stats" parent="offer_stats.menu_2"
              action="offer_stats.action_server"/>
    -->
  </data>
</odoo>